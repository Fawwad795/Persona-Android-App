<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PersonaApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="android:windowBackground">#EEEEEE</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorControlHighlight">#4B172A6D</item>
        <item name="colorControlActivated">@color/black</item>
        <item name="android:solidColor">@color/white</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.PersonaApp" parent="Base.Theme.PersonaApp" />

    <style name="Theme.PersonaApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PersonaApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PersonaApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
